# NewsletterForm
Subscribe panel. `tone="dark"` (black w/ red top rule) or `"light"` (paper). Composes Button + Kicker.

```jsx
<NewsletterForm heading="The week in Northeastern news" onSubmit={(e)=>console.log(e)} />
```
